import React from 'react';

class File extends React.Component {
  render() {
    return (
      <React.Fragment>
        <p> This is the File folder. </p>
      </React.Fragment>
    );
  } 
}

export default File;